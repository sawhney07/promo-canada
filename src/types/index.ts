export interface ButtonProps {
  label: string;
  onClick: () => void;
}

export interface WindowSize {
  width: number;
  height: number;
}

export interface HomeProps {
  title: string;
  description: string;
}